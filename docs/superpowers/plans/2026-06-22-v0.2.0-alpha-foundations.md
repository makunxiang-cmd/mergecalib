# v0.2.0 Alpha Foundations Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the v0.2.0-alpha technical foundation: classed conditions, province-blocked candidate distances, classed warnings, and stronger invariant tests.

**Architecture:** Add a condition module under `r-package/R/conditions.R`, keep generic helpers in `utils.R`, migrate error sites to stable class contracts, compute candidate distances inside each province loop, and emit conservative post-fit warnings through a centralized helper. Tests become behavior/class/invariant based instead of message-string based.

**Tech Stack:** R package, testthat 3, HiGHS, base R conditions, Matrix, existing hand-written Rd documentation.

---

## File Structure

- Create: `r-package/R/conditions.R`
  - Builds package error and warning objects.
  - Emits classed errors and warnings.
  - Holds warning option and threshold helpers.
- Modify: `r-package/R/utils.R`
  - Remove old unclassed `.mc_stop()` / `.mc_warn()` definitions after the new condition helpers are in place.
  - Keep generic utilities.
- Modify: `r-package/R/validate.R`
  - Replace validation failures with `mergecalib_error_input`, `mergecalib_error_target`, `mergecalib_error_structural`, and `mergecalib_error_not_estimable`.
- Modify: `r-package/R/spec.R`
  - Replace spec failures with `mergecalib_error_spec`.
- Modify: `r-package/R/candidates.R`
  - Replace full-dataset `.distance_matrix()` with province-blocked distance computation.
  - Replace candidate/weight-bound failures with classed errors.
- Modify: `r-package/R/model.R`
  - Replace internal/solver failures with classed errors where present.
- Modify: `r-package/R/fit.R`
  - Replace infeasible, solver, argument, and internal failures with classed errors.
  - Call post-fit warning helper after final audit succeeds.
- Modify: `r-package/R/results.R`
  - Replace fit-object validation failures with classed errors.
  - Add `.emit_fit_warnings(fit)` and warning detection helpers.
- Modify: `r-package/tests/testthat/test-validation.R`
  - Assert condition classes instead of message substrings.
- Modify: `r-package/tests/testthat/test-candidates.R`
  - Add blocked-distance invariant tests.
- Modify: `r-package/tests/testthat/test-fit.R`
  - Add infeasibility and warning integration tests.
- Create: `r-package/tests/testthat/test-conditions.R`
  - Tests condition constructor and emitter class hierarchies.
- Create: `r-package/tests/testthat/test-warnings.R`
  - Tests warning thresholds and suppression.
- Modify: `r-package/NEWS.md`
  - Add v0.2.0 development notes.
- Modify: `docs/maintainer/PROJECT_OVERVIEW.md`
  - Document conditions, warnings, and distance blocking.
- Modify: `docs/maintainer/ROADMAP_v0.2.0.md`
  - Mark alpha foundations as in progress or completed after implementation.

---

### Task 1: Baseline Verification

**Files:**
- Read-only: `r-package/tests/testthat/*.R`
- Read-only: `r-package/R/*.R`

- [ ] **Step 1: Confirm working tree and branch**

Run:

```bash
git status -sb
git branch --show-current
```

Expected:

```text
## main...origin/main
main
```

- [ ] **Step 2: Run existing unit tests**

Run:

```bash
cd r-package
Rscript -e 'devtools::test()'
```

Expected:

```text
[ FAIL 0 | WARN 0 | SKIP 0 | PASS 15 ]
```

- [ ] **Step 3: Confirm no generated baseline artifacts are needed**

Run:

```bash
git ls-files | rg 'rds|Rcheck|tar.gz|pdf|_中文|AGENTS' || true
```

Expected:

```text
No tracked generated/private artifacts are printed.
```

---

### Task 2: Add Classed Condition Infrastructure

**Files:**
- Create: `r-package/R/conditions.R`
- Modify: `r-package/R/utils.R`
- Create: `r-package/tests/testthat/test-conditions.R`

- [ ] **Step 1: Write failing condition tests**

Create `r-package/tests/testthat/test-conditions.R`:

```r
test_that("mergecalib errors carry specific and base classes", {
  err <- tryCatch(
    .mc_stop("mergecalib_error_input", "`data` must be a data frame."),
    error = identity
  )

  expect_s3_class(err, "mergecalib_error_input")
  expect_s3_class(err, "mergecalib_error")
  expect_s3_class(err, "error")
  expect_match(conditionMessage(err), "`data` must be a data frame.", fixed = TRUE)
})

test_that("mergecalib warnings carry specific and base classes", {
  warn <- NULL
  withCallingHandlers(
    .mc_warn("mergecalib_warning_relaxation", "Large relaxation was required."),
    warning = function(w) {
      warn <<- w
      invokeRestart("muffleWarning")
    }
  )

  expect_s3_class(warn, "mergecalib_warning_relaxation")
  expect_s3_class(warn, "mergecalib_warning")
  expect_s3_class(warn, "warning")
  expect_match(conditionMessage(warn), "Large relaxation was required.", fixed = TRUE)
})

test_that("mergecalib warnings obey the global warning switch", {
  old <- getOption("mergecalib.warn")
  on.exit(options(mergecalib.warn = old), add = TRUE)
  options(mergecalib.warn = FALSE)

  expect_warning(
    .mc_warn("mergecalib_warning_relaxation", "This warning is disabled."),
    NA
  )
})

test_that("warning thresholds are merged with defaults", {
  old <- getOption("mergecalib.warning_thresholds")
  on.exit(options(mergecalib.warning_thresholds = old), add = TRUE)
  options(mergecalib.warning_thresholds = list(max_weight_ratio = 2))

  thresholds <- .mc_warning_thresholds()
  expect_equal(thresholds$max_weight_ratio, 2)
  expect_equal(thresholds$relaxation_delta, 0.02)
  expect_equal(thresholds$near_binding_margin, 0.005)
})
```

- [ ] **Step 2: Run condition tests and verify RED**

Run:

```bash
cd r-package
Rscript -e 'devtools::test(filter = "conditions")'
```

Expected:

```text
FAIL because .mc_stop/.mc_warn still emit unclassed base conditions or .mc_warning_thresholds does not exist.
```

- [ ] **Step 3: Implement condition helpers**

Create `r-package/R/conditions.R`:

```r
.mc_condition_message <- function(...) {
  paste0(..., collapse = "")
}

.mc_error <- function(message, class, ..., call = NULL) {
  if (!is.character(class) || length(class) != 1L || !nzchar(class)) {
    stop("Internal error: condition class must be a non-empty string.", call. = FALSE)
  }
  structure(
    c(list(message = as.character(message), call = call), list(...)),
    class = unique(c(class, "mergecalib_error", "error", "condition"))
  )
}

.mc_stop <- function(class, ..., call = NULL) {
  stop(.mc_error(.mc_condition_message(...), class = class, call = call))
}

.mc_warning <- function(message, class, ..., call = NULL) {
  if (!is.character(class) || length(class) != 1L || !nzchar(class)) {
    stop("Internal error: warning class must be a non-empty string.", call. = FALSE)
  }
  structure(
    c(list(message = as.character(message), call = call), list(...)),
    class = unique(c(class, "mergecalib_warning", "warning", "condition"))
  )
}

.mc_warn <- function(class, ..., call = NULL) {
  if (identical(getOption("mergecalib.warn", TRUE), FALSE)) return(invisible(NULL))
  warning(.mc_warning(.mc_condition_message(...), class = class, call = call))
  invisible(NULL)
}

.mc_default_warning_thresholds <- function() {
  list(
    relaxation_delta = 0.02,
    max_weight_ratio = 5,
    heterogeneity = Inf,
    near_binding_margin = 0.005
  )
}

.mc_warning_thresholds <- function() {
  defaults <- .mc_default_warning_thresholds()
  user <- getOption("mergecalib.warning_thresholds", list())
  if (!is.list(user)) return(defaults)
  utils::modifyList(defaults, user)
}
```

Remove old `.mc_stop()` and `.mc_warn()` definitions from `r-package/R/utils.R`.

- [ ] **Step 4: Temporarily migrate all call sites to keep the suite loadable**

Run:

```bash
rg '\.mc_stop\(' r-package/R
```

For every existing call shaped like:

```r
.mc_stop("message")
```

change it to a conservative classed call:

```r
.mc_stop("mergecalib_error_internal", "message")
```

This temporary internal classification is narrowed in Tasks 3 and 4.

- [ ] **Step 5: Run condition tests and full tests**

Run:

```bash
cd r-package
Rscript -e 'devtools::test(filter = "conditions")'
Rscript -e 'devtools::test()'
```

Expected:

```text
Condition tests pass.
Full suite passes with 0 failures.
```

- [ ] **Step 6: Commit condition infrastructure**

Run:

```bash
git add r-package/R/conditions.R r-package/R/utils.R r-package/R r-package/tests/testthat/test-conditions.R
git commit -m "feat: add classed mergecalib conditions"
```

Expected:

```text
[main <hash>] feat: add classed mergecalib conditions
```

---

### Task 3: Migrate Validation and Spec Errors to Specific Classes

**Files:**
- Modify: `r-package/R/validate.R`
- Modify: `r-package/R/spec.R`
- Modify: `r-package/tests/testthat/test-validation.R`
- Modify/Create: `r-package/tests/testthat/test-conditions.R`

- [ ] **Step 1: Write failing validation class tests**

Replace `r-package/tests/testthat/test-validation.R` with:

```r
test_that("all-zero province is rejected with structural class", {
  dat <- example_merge_data()
  dat$n[dat$province == "P1"] <- 0
  dat$A[dat$province == "P1"] <- 0
  dat$B[dat$province == "P1"] <- 0
  dat$C[dat$province == "P1"] <- 0
  dat$D[dat$province == "P1"] <- 0

  expect_error(
    validate_merge_data(dat),
    class = "mergecalib_error_structural"
  )
})

test_that("grade counts must sum to n with input class", {
  dat <- example_merge_data()
  dat$A[1] <- dat$A[1] + 1

  expect_error(
    validate_merge_data(dat),
    class = "mergecalib_error_input"
  )
})

test_that("unestimable targets are rejected with not-estimable class", {
  dat <- example_merge_data()
  dat$n[dat$sex == "F"] <- 0
  dat$A[dat$sex == "F"] <- 0
  dat$B[dat$sex == "F"] <- 0
  dat$C[dat$sex == "F"] <- 0
  dat$D[dat$sex == "F"] <- 0
  targets <- data.frame(
    target_id = "female",
    grade = "A",
    lower = 0.1,
    upper = 0.9,
    province = NA,
    sex = "F",
    urban = NA,
    age = NA,
    education = NA,
    stringsAsFactors = FALSE
  )

  expect_error(
    validate_merge_data(dat, targets),
    class = "mergecalib_error_not_estimable"
  )
})
```

Append to `r-package/tests/testthat/test-conditions.R`:

```r
test_that("merge_spec validation failures use spec class", {
  expect_error(
    merge_spec(grades = character()),
    class = "mergecalib_error_spec"
  )
})
```

- [ ] **Step 2: Run targeted tests and verify RED**

Run:

```bash
cd r-package
Rscript -e 'devtools::test(filter = "validation|conditions")'
```

Expected:

```text
FAIL because validation/spec paths are still classified as mergecalib_error_internal.
```

- [ ] **Step 3: Classify validation errors**

In `r-package/R/validate.R`, map failures:

```r
# input/schema/count failures
.mc_stop("mergecalib_error_input", "...")

# target table shape, duplicate target_id/grade, inconsistent intervals
.mc_stop("mergecalib_error_target", "...")

# all-zero province
.mc_stop("mergecalib_error_structural", "...")

# target matched rows but total observed sample is zero
.mc_stop("mergecalib_error_not_estimable", "...")
```

Concrete replacements:

- `data` not data frame or missing required columns -> `mergecalib_error_input`
- duplicate cell identifier -> `mergecalib_error_input`
- missing demographic defining variables -> `mergecalib_error_input`
- duplicated demographic combinations -> `mergecalib_error_input`
- invalid `n`, `weight`, grade counts, or grade sums -> `mergecalib_error_input`
- all-zero provinces -> `mergecalib_error_structural`
- invalid `targets` object, missing columns, invalid grade/intervals -> `mergecalib_error_target`
- target row matches no original cell -> `mergecalib_error_target`
- target population with zero observed sample -> `mergecalib_error_not_estimable`

- [ ] **Step 4: Classify spec errors**

In `r-package/R/spec.R`, every spec-construction failure becomes:

```r
.mc_stop("mergecalib_error_spec", "...")
```

- [ ] **Step 5: Run targeted and full tests**

Run:

```bash
cd r-package
Rscript -e 'devtools::test(filter = "validation|conditions")'
Rscript -e 'devtools::test()'
```

Expected:

```text
Validation and condition tests pass.
Full suite passes with 0 failures.
```

- [ ] **Step 6: Commit validation/spec migration**

Run:

```bash
git add r-package/R/validate.R r-package/R/spec.R r-package/tests/testthat/test-validation.R r-package/tests/testthat/test-conditions.R
git commit -m "feat: classify validation and spec failures"
```

Expected:

```text
[main <hash>] feat: classify validation and spec failures
```

---

### Task 4: Province-Block Candidate Distances

**Files:**
- Modify: `r-package/R/candidates.R`
- Modify: `r-package/tests/testthat/test-candidates.R`
- Create: `r-package/tests/testthat/test-distance-blocking.R`

- [ ] **Step 1: Write failing blocked-distance tests**

Create `r-package/tests/testthat/test-distance-blocking.R`:

```r
test_that("blocked candidate generation preserves deterministic small fixture", {
  spec <- merge_spec(level_orders = list(
    age = c("18-39", "40+"),
    education = c("low", "high")
  ))
  dat <- example_merge_data()[1:8, , drop = FALSE]

  cand <- generate_candidate_clusters(
    dat, spec,
    max_cluster_size = 3,
    max_neighbors = 3
  )

  expect_equal(
    cand$cluster_key,
    c(
      "1", "1:2", "1:2:3", "1:2:4", "1:2:5", "1:3", "1:3:4",
      "1:3:5", "1:5", "2", "2:3", "2:3:4", "2:4", "2:4:8",
      "2:5:6", "2:6", "2:6:8", "3:4", "3:4:8", "3:5:7",
      "3:7", "3:7:8", "4", "4:8", "5", "5:6", "5:6:7",
      "5:6:8", "5:7", "5:7:8", "5:8", "6", "6:7:8", "6:8",
      "7", "7:8", "8"
    )
  )
  expect_equal(
    cand$n_total,
    c(
      12, 31, 31, 47, 37, 12, 28, 18, 18, 19, 19, 35, 35, 45,
      38, 32, 42, 16, 26, 26, 20, 30, 16, 26, 6, 19, 39, 29,
      26, 36, 16, 13, 43, 23, 20, 30, 10
    )
  )
  expect_equal(
    cand$demo_distance,
    c(
      0, 12, 14, 28, 30, 1, 25, 13, 12, 0, 2, 18, 16, 46,
      44, 26, 56, 1, 21, 8, 2, 12, 0, 20, 0, 6, 32, 16,
      6, 16, 12, 0, 36, 10, 0, 10, 0
    )
  )
  expect_true(all(cand$n_total > 0))
})

test_that("distance block helper only returns province-sized matrices", {
  spec <- merge_spec(level_orders = list(
    age = c("18-39", "40+"),
    education = c("low", "high")
  ))
  dat <- example_merge_data()
  p1 <- dat[dat$province == "P1", , drop = FALSE]

  dist <- .distance_matrix_block(p1, spec)

  expect_equal(dim(dist), c(nrow(p1), nrow(p1)))
  expect_true(all(diag(dist) == 0))
})
```

- [ ] **Step 2: Run distance tests and verify RED**

Run:

```bash
cd r-package
Rscript -e 'devtools::test(filter = "distance-blocking")'
```

Expected:

```text
FAIL because .distance_matrix_block does not exist.
```

- [ ] **Step 3: Implement `.distance_matrix_block()`**

In `r-package/R/candidates.R`, rename `.distance_matrix()` to:

```r
.distance_matrix_block <- function(data, spec, distance_weights = NULL) {
  group_names <- names(spec$groups)
  if (is.null(distance_weights)) {
    distance_weights <- stats::setNames(rep(1, length(group_names)), group_names)
    distance_weights[setdiff(group_names, spec$ordered_groups)] <- 2
  }
  missing_weights <- setdiff(group_names, names(distance_weights))
  if (length(missing_weights)) distance_weights[missing_weights] <- 1

  nr <- nrow(data)
  out <- matrix(0, nr, nr)
  for (g in group_names) {
    col <- spec$groups[[g]]
    z <- .encode_group(data[[col]], g, spec)
    if (g %in% spec$ordered_groups) {
      component <- abs(outer(z, z, "-"))
    } else {
      component <- outer(z, z, FUN = function(a, b) as.numeric(a != b))
    }
    out <- out + as.numeric(distance_weights[[g]]) * component
  }
  diag(out) <- 0
  out
}
```

- [ ] **Step 4: Refactor province loop to use local distances**

In `generate_candidate_clusters()`:

1. Remove:

```r
dist_mat <- .distance_matrix(data, spec, distance_weights)
```

2. Inside each province loop, add:

```r
block <- data[idx, , drop = FALSE]
dist_mat <- .distance_matrix_block(block, spec, distance_weights)
local_to_global <- idx
global_to_local <- seq_along(idx)
names(global_to_local) <- as.character(idx)
```

3. Convert local members before adding:

```r
members_global <- local_to_global[members_local]
store <- .add_candidate(store, members_global, data, spec, dist_lookup, ...)
```

Use a helper to avoid passing a full national matrix:

```r
.dist_lookup_block <- function(dist_mat, global_to_local) {
  function(i, j) dist_mat[global_to_local[as.character(i)], global_to_local[as.character(j)]]
}
```

Then update `.cluster_metrics()` and `.combination_candidates()` to accept a
distance lookup function:

```r
demo_distance <- sum(pmax(n, 1) * dist(members, anchor))
score <- vapply(cmb, function(z) {
  sum(dist(seed, z)) + 1e-9 * sum(data[[spec$n]][z])
}, numeric(1))
```

- [ ] **Step 5: Classify candidate errors**

In `r-package/R/candidates.R`, use:

```r
.mc_stop("mergecalib_error_candidate", "Candidate parameters must satisfy ...")
.mc_stop("mergecalib_error_weight_bounds", "Zero-sample cells in province `", p, "` cannot be absorbed ...")
.mc_stop("mergecalib_error_candidate", "No candidate merged clusters were generated.")
.mc_stop("mergecalib_error_candidate", "The following original cells are not covered ...")
.mc_stop("mergecalib_error_internal", "Internal error: a candidate cluster with sample size 0 was produced.")
```

- [ ] **Step 6: Run targeted and full tests**

Run:

```bash
cd r-package
Rscript -e 'devtools::test(filter = "candidate|distance-blocking")'
Rscript -e 'devtools::test()'
```

Expected:

```text
Candidate and distance-blocking tests pass.
Full suite passes with 0 failures.
```

- [ ] **Step 7: Commit distance blocking**

Run:

```bash
git add r-package/R/candidates.R r-package/tests/testthat/test-candidates.R r-package/tests/testthat/test-distance-blocking.R
git commit -m "feat: compute candidate distances by province block"
```

Expected:

```text
[main <hash>] feat: compute candidate distances by province block
```

---

### Task 5: Migrate Fit, Model, and Result Errors

**Files:**
- Modify: `r-package/R/fit.R`
- Modify: `r-package/R/model.R`
- Modify: `r-package/R/results.R`
- Modify: `r-package/R/utils.R`
- Modify: `r-package/tests/testthat/test-fit.R`
- Modify: `r-package/tests/testthat/test-conditions.R`

- [ ] **Step 1: Write failing class tests for fit/model/result paths**

Append to `r-package/tests/testthat/test-fit.R`:

```r
test_that("invalid max_delta uses input class", {
  skip_if_not_installed("highs")
  dat <- example_merge_data()
  spec <- merge_spec(level_orders = list(
    age = c("18-39", "40+"),
    education = c("low", "high")
  ))
  targets <- example_merge_targets(dat, spec)

  expect_error(
    fit_merge_calibration(dat, targets, spec, max_delta = 2),
    class = "mergecalib_error_input"
  )
})

test_that("disabled relaxation uses infeasible class when strict targets fail", {
  skip_if_not_installed("highs")
  dat <- example_merge_data()
  spec <- merge_spec(level_orders = list(
    age = c("18-39", "40+"),
    education = c("low", "high")
  ))
  targets <- example_merge_targets(dat, spec, half_width = 0)

  expect_error(
    fit_merge_calibration(
      dat, targets, spec,
      relax_targets = FALSE,
      solver_control = list(time_limit = 60)
    ),
    class = "mergecalib_error_infeasible"
  )
})
```

Append to `r-package/tests/testthat/test-conditions.R`:

```r
test_that("result accessors reject non-fit objects with input class", {
  expect_error(
    audit_merge_fit(list()),
    class = "mergecalib_error_input"
  )
})
```

- [ ] **Step 2: Run targeted tests and verify RED**

Run:

```bash
cd r-package
Rscript -e 'devtools::test(filter = "fit|conditions")'
```

Expected:

```text
FAIL because fit/result paths still use mergecalib_error_internal.
```

- [ ] **Step 3: Classify fit errors**

In `r-package/R/fit.R`, map failures:

```r
# invalid user arguments
.mc_stop("mergecalib_error_input", "...")

# unknown objective column or non-finite objective
.mc_stop("mergecalib_error_input", "...")

# solver did not return feasible solution or violated constraints
.mc_stop("mergecalib_error_solver", "...")

# strict infeasible with relax_targets = FALSE or max_delta infeasible
.mc_stop("mergecalib_error_infeasible", "...")

# selected no clusters, non-positive selected cluster, exact-cover failure, final audit failure
.mc_stop("mergecalib_error_internal", "...")
```

- [ ] **Step 4: Classify model errors**

In `r-package/R/model.R`, map:

```r
.mc_stop("mergecalib_error_internal", "Internal error: objective length ...")
```

If `.solve_highs()` catches or detects solver failures in implementation, use:

```r
.mc_stop("mergecalib_error_solver", "...")
```

- [ ] **Step 5: Classify result errors**

In `r-package/R/results.R`, map non-fit object errors to:

```r
.mc_stop("mergecalib_error_input", "`fit` must be a mergecalib_fit object.")
```

- [ ] **Step 6: Classify dependency assertion**

In `r-package/R/utils.R`, update `.assert_installed()`:

```r
.assert_installed <- function(package) {
  if (!requireNamespace(package, quietly = TRUE)) {
    .mc_stop(
      "mergecalib_error_input",
      "The R package '", package, "' is required to solve the model. ",
      "Please run install.packages(\"", package, "\")."
    )
  }
}
```

- [ ] **Step 7: Ensure no bare internal migration remains**

Run:

```bash
rg 'mergecalib_error_internal' r-package/R
```

Expected: only genuine internal invariant failures remain.

- [ ] **Step 8: Run targeted and full tests**

Run:

```bash
cd r-package
Rscript -e 'devtools::test(filter = "fit|conditions")'
Rscript -e 'devtools::test()'
```

Expected:

```text
Fit and condition tests pass.
Full suite passes with 0 failures.
```

- [ ] **Step 9: Commit error migration**

Run:

```bash
git add r-package/R/fit.R r-package/R/model.R r-package/R/results.R r-package/R/utils.R r-package/tests/testthat/test-fit.R r-package/tests/testthat/test-conditions.R
git commit -m "feat: classify fit model and result failures"
```

Expected:

```text
[main <hash>] feat: classify fit model and result failures
```

---

### Task 6: Add Post-Fit Warning Layer

**Files:**
- Modify: `r-package/R/results.R`
- Modify: `r-package/R/fit.R`
- Create: `r-package/tests/testthat/test-warnings.R`

- [ ] **Step 1: Write failing warning tests**

Create `r-package/tests/testthat/test-warnings.R`:

```r
test_that("weight-ratio warnings fire when threshold is low", {
  skip_if_not_installed("highs")
  old_warn <- getOption("mergecalib.warn")
  old_thresholds <- getOption("mergecalib.warning_thresholds")
  on.exit({
    options(mergecalib.warn = old_warn)
    options(mergecalib.warning_thresholds = old_thresholds)
  }, add = TRUE)

  options(
    mergecalib.warn = TRUE,
    mergecalib.warning_thresholds = list(max_weight_ratio = 1)
  )

  dat <- example_merge_data()
  spec <- merge_spec(level_orders = list(
    age = c("18-39", "40+"),
    education = c("low", "high")
  ))
  targets <- example_merge_targets(dat, spec, half_width = 0.03)

  expect_warning(
    fit_merge_calibration(
      dat, targets, spec,
      solver_control = list(time_limit = 60)
    ),
    class = "mergecalib_warning_weight_distortion"
  )
})

test_that("mergecalib.warn disables post-fit warnings", {
  skip_if_not_installed("highs")
  old_warn <- getOption("mergecalib.warn")
  old_thresholds <- getOption("mergecalib.warning_thresholds")
  on.exit({
    options(mergecalib.warn = old_warn)
    options(mergecalib.warning_thresholds = old_thresholds)
  }, add = TRUE)

  options(
    mergecalib.warn = FALSE,
    mergecalib.warning_thresholds = list(max_weight_ratio = 1)
  )

  dat <- example_merge_data()
  spec <- merge_spec(level_orders = list(
    age = c("18-39", "40+"),
    education = c("low", "high")
  ))
  targets <- example_merge_targets(dat, spec, half_width = 0.03)

  expect_warning(
    fit_merge_calibration(
      dat, targets, spec,
      solver_control = list(time_limit = 60)
    ),
    NA
  )
})

test_that("near-binding warnings can be triggered with a wide margin", {
  skip_if_not_installed("highs")
  old_warn <- getOption("mergecalib.warn")
  old_thresholds <- getOption("mergecalib.warning_thresholds")
  on.exit({
    options(mergecalib.warn = old_warn)
    options(mergecalib.warning_thresholds = old_thresholds)
  }, add = TRUE)

  options(
    mergecalib.warn = TRUE,
    mergecalib.warning_thresholds = list(near_binding_margin = 1)
  )

  dat <- example_merge_data()
  spec <- merge_spec(level_orders = list(
    age = c("18-39", "40+"),
    education = c("low", "high")
  ))
  targets <- example_merge_targets(dat, spec, half_width = 0.03)

  expect_warning(
    fit_merge_calibration(
      dat, targets, spec,
      solver_control = list(time_limit = 60)
    ),
    class = "mergecalib_warning_near_binding"
  )
})
```

- [ ] **Step 2: Run warning tests and verify RED**

Run:

```bash
cd r-package
Rscript -e 'devtools::test(filter = "warnings")'
```

Expected:

```text
FAIL because .emit_fit_warnings() is not implemented and fit_merge_calibration() emits no post-fit warnings.
```

- [ ] **Step 3: Implement `.emit_fit_warnings(fit)`**

Add to `r-package/R/results.R`:

```r
.emit_fit_warnings <- function(fit) {
  if (identical(getOption("mergecalib.warn", TRUE), FALSE)) return(invisible(NULL))
  thresholds <- .mc_warning_thresholds()
  final <- final_cells(fit)

  if (is.finite(thresholds$relaxation_delta) &&
      isTRUE(fit$delta > thresholds$relaxation_delta)) {
    .mc_warn(
      "mergecalib_warning_relaxation",
      "Minimum uniform target relaxation is ",
      .compact_number(fit$delta),
      ", above the warning threshold ",
      .compact_number(thresholds$relaxation_delta),
      "."
    )
  }

  max_ratio <- max(final$max_weight_ratio)
  if (is.finite(thresholds$max_weight_ratio) &&
      isTRUE(max_ratio > thresholds$max_weight_ratio)) {
    .mc_warn(
      "mergecalib_warning_weight_distortion",
      "Maximum final weight ratio is ",
      .compact_number(max_ratio),
      ", above the warning threshold ",
      .compact_number(thresholds$max_weight_ratio),
      "."
    )
  }

  max_heterogeneity <- max(final$heterogeneity)
  if (is.finite(thresholds$heterogeneity) &&
      isTRUE(max_heterogeneity > thresholds$heterogeneity)) {
    .mc_warn(
      "mergecalib_warning_heterogeneity",
      "Maximum final cluster heterogeneity is ",
      .compact_number(max_heterogeneity),
      ", above the warning threshold ",
      .compact_number(thresholds$heterogeneity),
      "."
    )
  }

  tr <- .compute_target_results(fit)
  margin <- pmin(
    abs(tr$final_proportion - tr$effective_lower),
    abs(tr$effective_upper - tr$final_proportion)
  )
  if (is.finite(thresholds$near_binding_margin) &&
      any(margin <= thresholds$near_binding_margin, na.rm = TRUE)) {
    .mc_warn(
      "mergecalib_warning_near_binding",
      "At least one target is within ",
      .compact_number(thresholds$near_binding_margin),
      " of an effective interval boundary."
    )
  }

  invisible(NULL)
}
```

- [ ] **Step 4: Call warning helper after final audit**

In `r-package/R/fit.R`, after final audit succeeds and before returning `fit`:

```r
  if (!isTRUE(audit$valid)) {
    .mc_stop("mergecalib_error_internal", "Final audit failed: ", paste(audit$issues, collapse = "; "), ".")
  }
  .emit_fit_warnings(fit)
  fit
```

- [ ] **Step 5: Run warning and full tests**

Run:

```bash
cd r-package
Rscript -e 'devtools::test(filter = "warnings|fit")'
Rscript -e 'devtools::test()'
```

Expected:

```text
Warning tests pass.
Full suite passes with 0 failures and ordinary example tests do not emit unexpected warnings.
```

- [ ] **Step 6: Commit warning layer**

Run:

```bash
git add r-package/R/results.R r-package/R/fit.R r-package/tests/testthat/test-warnings.R
git commit -m "feat: add classed fit warnings"
```

Expected:

```text
[main <hash>] feat: add classed fit warnings
```

---

### Task 7: Documentation, Version, and Release Checks

**Files:**
- Modify: `r-package/DESCRIPTION`
- Modify: `r-package/NEWS.md`
- Modify: `docs/maintainer/PROJECT_OVERVIEW.md`
- Modify: `docs/maintainer/ROADMAP_v0.2.0.md`
- Modify: `r-package/man/results.Rd` if warning behavior is documented there.

- [ ] **Step 1: Update package development version**

In `r-package/DESCRIPTION`, change:

```text
Version: 0.1.0
```

to:

```text
Version: 0.2.0.9000
```

- [ ] **Step 2: Update NEWS**

Prepend to `r-package/NEWS.md`:

```markdown
# mergecalib 0.2.0.9000

- Added classed mergecalib error and warning conditions for v0.2.0 development.
- Reworked candidate generation to compute demographic distances by province
  block instead of allocating a full-dataset distance matrix.
- Added conservative post-fit warnings for large relaxation, high weight
  distortion, high heterogeneity, and near-binding targets.
- Expanded tests around condition classes, candidate invariants, and warning
  controls.

# mergecalib 0.1.0
```

- [ ] **Step 3: Update maintainer project overview**

In `docs/maintainer/PROJECT_OVERVIEW.md`, add a short "v0.2.0 Alpha Foundation"
section:

```markdown
## v0.2.0 Alpha Foundation

The v0.2.0 development line introduces classed package conditions, conservative
post-fit warning hooks, and province-blocked candidate distance computation.
These changes preserve positive-final-sample, no-cross-province-borrowing,
determinism, and conservation invariants while replacing message-string tests
with stable condition-class contracts.
```

- [ ] **Step 4: Update roadmap status**

In `docs/maintainer/ROADMAP_v0.2.0.md`, mark the foundation items as started or
complete:

```markdown
**0.2.0-alpha status:** Foundation work has landed for A3, C1, C2, and the
initial E test scaffold. Remaining v0.2.0-beta work starts from A1/A2/B1/B2.
```

- [ ] **Step 5: Run full tests**

Run:

```bash
cd r-package
Rscript -e 'devtools::test()'
```

Expected:

```text
[ FAIL 0 | WARN 0 | SKIP 0 | PASS <updated-count> ]
```

- [ ] **Step 6: Build package**

Run:

```bash
cd r-package
R CMD build .
```

Expected:

```text
* checking for file './DESCRIPTION' ... OK
* building 'mergecalib_0.2.0.9000.tar.gz'
```

- [ ] **Step 7: Run CRAN-style check**

Run:

```bash
cd r-package
R CMD check --as-cran --no-manual mergecalib_0.2.0.9000.tar.gz
```

Expected:

```text
Status: 0 errors | 0 warnings
```

Known acceptable notes must be copied into the final implementation summary.

- [ ] **Step 8: Verify no generated artifacts are tracked**

Run:

```bash
git status --ignored -sb
git ls-files | rg 'tar.gz|Rcheck|docs/user-manual|AGENTS|_中文' || true
```

Expected:

```text
Generated tarballs and .Rcheck directories appear only under ignored output.
The git ls-files command prints no tracked generated/private artifacts.
```

- [ ] **Step 9: Commit final alpha foundations**

Run:

```bash
git add r-package/DESCRIPTION r-package/NEWS.md docs/maintainer/PROJECT_OVERVIEW.md docs/maintainer/ROADMAP_v0.2.0.md r-package/man/results.Rd
git commit -m "docs: document v0.2.0 alpha foundations"
```

Expected:

```text
[main <hash>] docs: document v0.2.0 alpha foundations
```

---

## Plan Self-Review

- Spec coverage: This plan covers A3, C1, C2, and E from the approved
  v0.2.0-alpha foundations spec. It intentionally excludes A1/A2/B1/B2 and
  other beta/polish workstreams.
- Incomplete-marker scan: No incomplete tasks remain. Each task contains concrete
  files, test code, implementation shape, commands, and expected results.
- Type and naming consistency: Condition classes use the
  `mergecalib_error_*` and `mergecalib_warning_*` names from the approved spec.
  Warning threshold keys match the spec: `relaxation_delta`,
  `max_weight_ratio`, `heterogeneity`, and `near_binding_margin`.
